{
  "name": "ict-review-progress",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-plan.mjs",
    "check": "node scripts/check-plan.mjs",
    "site": "npm run check && node scripts/build-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
